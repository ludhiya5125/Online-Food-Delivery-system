package RestaurantManagementModule;

import java.util.HashMap;
import java.util.Map;

public class RestaurantManagement
{
   /*private Map<String, Restaurant> restaurantList;
    public RestaurantManagement()
    {
        restaurantList=new HashMap<>();
    }
    //Adding the restaurant
    public void addRestaurant(String name,String cusineType, String location)
    {
        if (!restaurantList.containsKey(name))
        {
            Restaurant restaurant = new Restaurant(name,cusineType,location);
            restaurantList.put(name, restaurant);
            System.out.println("Restaurant added successfully!");
        } else {
            System.out.println("Restaurant with the same name already exists.");
        }
    }

    // Method to update restaurant details
    public void updateRestaurant(String name, String location)
    {
        if (restaurantList.containsKey(name)) {
            Restaurant restaurant = restaurantList.get(name);
            restaurant.location = location;
            System.out.println("Restaurant details updated successfully!");
        } else {
            throw new RestaurantNotFoundException("Restaurant not found.");
        }
    }

    // Method to deactivate a restaurant
    public void deactivateRestaurant(String name)
    {
        if (restaurantList.containsKey(name)) {
            Restaurant restaurant = restaurantList.get(name);
            restaurant.deactive();
            System.out.println("Restaurant deactivated successfully!");
        } else {
            throw new RestaurantNotFoundException("Restaurant not found.");
        }
    }

    // Method to display details of all restaurants
    public void displayAllRestaurants() {
        System.out.println("All Restaurants:");
        for (Restaurant restaurant : restaurantList.values()) {
            restaurant.display();
            System.out.println();
        }*/
    }


